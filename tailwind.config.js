/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}",], //all the files where tailwind css can be used, file with the following extensions in the src folder kinda regex
  theme: {
    extend: {},
  },
  plugins: [],
}

